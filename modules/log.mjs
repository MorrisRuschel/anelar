'use strict';

const DEBUG = false;

/*export default class console
{
    DEBUG = false;

    log()
    {
        if ( !DEBUG )
        {
            const methods = ['log', 'debug', 'warn', 'info'];

            for ( let index = 0; index < methods.length; index++ )
            {
                console[ methods[ index ] ] = function(){};
            }
        }
    }
}*/